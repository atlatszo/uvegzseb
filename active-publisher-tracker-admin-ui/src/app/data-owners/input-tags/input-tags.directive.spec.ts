import { InputTagsDirective } from './input-tags.directive';

describe('InputTagsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTagsDirective();
    expect(directive).toBeTruthy();
  });
});
