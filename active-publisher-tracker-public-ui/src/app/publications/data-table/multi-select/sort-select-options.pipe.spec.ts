import { SortSelectOptionsPipe } from './sort-select-options.pipe';

describe('SortSelectOptionsPipe', () => {

  it('create an instance', () => {
    const pipe = new SortSelectOptionsPipe();
    expect(pipe).toBeTruthy();
  });

});
